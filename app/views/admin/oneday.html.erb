<div class="margin-top"></div><!--nav margin-->
<div class="container">
	<!--intro-->
    <div class="row">
        <div class="col-sm-12">
            <h2 class="page-header darkgray">관리자 페이지 <small>원데이 클래스 노출 설정</small></h2>
        </div>
    </div>
    <!--/.intro-->
	<br/><br/>
	
	<!--Content -->
    <div class="row">
        <div class="col-sm-4">
        </div>
        <div class="col-sm-4">
            <div class="control-container-center">
                <%= bootstrap_form_for(@admin, url: {action: "update_oneday"}, method: "post") do |f| %>
                    <% if @admin.oneday == true %>
                        <%= f.radio_button :oneday, true, label:"보이기", :checked => true %>
                        <%= f.radio_button :oneday, false, label: "숨기기" %>
                    <% else %>
                        <%= f.radio_button :oneday, true, label:"보이기" %>
                        <%= f.radio_button :oneday, false, label: "숨기기", :checked => true %>
                    <% end %>
 
                    <%= f.submit "확인" %>
                <% end %>
            </div>
        </div> 
    </div>
    <!--/.Content-->