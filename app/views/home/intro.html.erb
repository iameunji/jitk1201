<div class="margin-top"></div><!--nav margin-->
<div class="container">
    <!--intro-->
    <div class="row">
        <div class="col-sm-12">
            <h2 class="page-header darkgray">Intro <small>정 in the Kitchen</small></h2>
        </div>
    </div>
    <!-- /.intro-->
    <br/>
    <br/>
    <!-- intro section -->
    <div class="row">
        <!-- intro image -->
        <section>
            <div class="col-sm-12 control-label-center">
                <%= image_tag Admin.intro_image.url , :width =>"100%", :height =>"300px", :class =>"margin-bottom" %>
                <% unless current_user.nil? %>
                    <% if current_user.admin==true %>
                <!--for admin-->
                <div class="control-label-right">
                    <a class="btn btn-default" href="/admin/modify">intro 이미지 수정하기</a>
                    <p>intro 대표사진 권장 사이즈는 1130px*300px 입니다</p>
                </div>
                <!--/.for admin-->
                <% end %>
            <% end %>

            </div>
        </section>
        <!--/.intro image-->
        <!--intro wording-->
        <section>
            <div class="col-sm-12 control-container-left">
                <p><%= @admin.intro_text %></p>

                <% unless current_user.nil? %>
                    <% if current_user.admin==true %>
                    <!--for admin-->
                    <div class="control-label-right">
                        <a class="btn btn-default" href="/admin/modifY">intro 내용 수정하기</a>
                    </div>
                    <!--/.for admin-->
                    <% end %>
                <% end %>

            </div>
        </section>
        <!--/.intro wording-->
    </div>
    <!--/.intro section -->
</div>
<!--/.container-->


